<?xml version="1.0" encoding="utf-8"?>
<!-- View IDs that are no longer defined in layout XML files but are still used at runtime
     (e.g. as AndroidView IDs in Compose screens or as origin-view identifiers for dialogs). -->
<resources>
    <!-- RichText AndroidView IDs used in NoteScreen.kt / NoteFragmentCompose -->
    <item name="title_edit" type="id"/>
    <item name="content_edit" type="id"/>
    <!-- Legacy bottom toolbar ID retained for AppSnackbar anchor lookup -->
    <item name="bottom_toolbar" type="id"/>
    <!-- ComposeView root IDs — assigned in Fragment.onCreateView so tests can detect navigation -->
    <item name="fragment_book_view_flipper" type="id"/>
    <item name="fragment_query_search_view_flipper" type="id"/>
    <item name="fragment_query_agenda_view_flipper" type="id"/>
</resources>
